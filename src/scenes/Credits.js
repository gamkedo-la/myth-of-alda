/* global Phaser */
import { Credits } from '../globals/SceneKeys.js'

class CreditsScene extends Phaser.Scene {
  constructor () {
    super({ key: Credits })
  }

  preload () {
  }

  create () {
  }
}

export default CreditsScene