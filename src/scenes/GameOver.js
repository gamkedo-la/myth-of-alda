/* global Phaser */
import { GameOver } from '../globals/SceneKeys.js'

class GameOverScene extends Phaser.Scene {
  constructor () {
    super({ key: GameOver })
  }

  preload () {
  }

  create () {
  }
}

export default GameOverScene