/* global Phaser */
import { Options } from '../globals/SceneKeys.js'

class OptionsScene extends Phaser.Scene {
  constructor () {
    super({ key: Options })
  }

  preload () {
  }

  create () {
  }
}

export default OptionsScene