/* global Phaser */
import { Title } from '../globals/SceneKeys.js'

class TitleScene extends Phaser.Scene {
  constructor () {
    super({ key: Title })
  }

  preload () {
  }

  create () {
  }
}

export default TitleScene