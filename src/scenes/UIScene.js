/* global Phaser */
import { UISceneKey } from '../globals/SceneKeys.js'

class UIScene extends Phaser.Scene {
  constructor () {
    super({ key: UISceneKey })
  }

  preload () {
  }

  create () {
  }
}

export default UIScene