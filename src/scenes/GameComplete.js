/* global Phaser */
import { GameComplete } from '../globals/SceneKeys.js'

class GameCompleteScene extends Phaser.Scene {
  constructor () {
    super({ key: GameComplete })
  }

  preload () {
  }

  create () {
  }
}

export default GameCompleteScene